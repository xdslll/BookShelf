<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="read.xiaotu.com.bookshelf">

    <uses-sdk android:minSdkVersion="15"
        android:targetSdkVersion="19"
        android:maxSdkVersion="22"/>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application android:allowBackup="true" android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher" android:theme="@android:style/Theme.Holo.Light">
        <activity android:name=".ImageViewDragActivity">
        </activity>
        <activity android:name=".grid._GridViewDragActivity">
        </activity>
        <activity android:name=".grid.DragGridViewActivity">

        </activity>
        <activity android:name=".viewgroup.TestViewGroupActivity">
        </activity>
        <activity android:name=".mygrid.MyGridViewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".animation.AnimationTestActivity">

        </activity>
        <activity android:name=".view.TestViewActivity">

        </activity>
    </application>

</manifest>
